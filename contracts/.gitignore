node_modules
.env
coverage
coverage.json
typechain
typechain-types

# Hardhat files
cache
artifacts

# Deployments (keep config but ignore timestamped deployments)
deployments/*.json
!deployments/.gitkeep
